<h1><%= @band.name %></h1>
<h2>A <%= @band.genre %> band founded in <%= @band.founded %></h2>
<h3>Currently Active: <%= @band.currently_active %></h3>
<h3><%= Musician.bandmate_count(@band.id) %> musician records available.</h3>
<p><a href="/bands/<%= "#{@band.id}" %>/musicians">see all musicians in <%= "#{@band.name}" %></a></p>

<p><%= link_to "Edit Band", "/bands/#{@band.id}/edit" %> | <%= link_to "Delete Band", "/bands/#{@band.id}", method: :delete %></p>

<p>record #<%= @band.id %>
<p>record created <%= @band.created_at %></p>
<p>udated <%= @band.updated_at %></p>
